# Audit Validado — Páginas Públicas Script-Master

**Data:** 26/04/2026  
**Versão:** 0.24.4  
**Escopo:** `src/pages/public/` (10 páginas) + `src/components/public/` (14 componentes) + helpers SEO/theme  
**Auditores originais:** audit-technical, audit-a11y, audit-ux-flow, audit-ui-design, audit-best-practices  
**Validadores:** 2 rodadas independentes (validator-audit A + B)

## Resumo

| Prioridade | Total |
|------------|-------|
| **P1 (Warning)** | 6 |
| **P2 (Suggestion)** | 11 |
| **P3 (Nitpick)** | 6 |
| **Total** | **23** |

> Após dedupulação + 2 rodadas de validação: 5 falsos positivos eliminados, 3 reclassificações aplicadas. Taxa de acerto final: **85% dos 27 findings originais confirmados**.

---

## P1 — Warning

### P1-1: FAQAccordion sem `id` e `aria-controls` no AccordionSummary

- **Arquivo:** `src/components/public/FAQAccordion.tsx:105-113`
- **WCAG:** 4.1.2 Name, Role, Value (Level A)
- **Problema:** O `AccordionSummary` não recebe `id` nem `aria-controls`, impedindo que screen readers associem o cabeçalho ao painel expansível. MUI v9 **não** gera esses atributos automaticamente (confirmado via NotebookLM).
- **Impacto:** Usuários de screen reader não conseguem identificar a relação entre o botão do accordion e seu conteúdo expandido.
- **Fix:**
  ```tsx
  const headingId = `faq-heading-${index}`;
  const panelId = `faq-panel-${index}`;
  <AccordionSummary id={headingId} aria-controls={panelId} ...>
  <AccordionDetails id={panelId} role="region" aria-labelledby={headingId}>
  ```

### P1-2: CTA "Começar Grátis" da PricingPage aponta para `/login`

- **Arquivo:** `src/pages/public/PricingPage.tsx:392` e `:476`
- **Problema:** O CTA "Começar Grátis" aponta para `/login`. A LandingPage faz certo (`/cadastro`), mas PricingPage envia o visitante sem conta para login.
- **Impacto:** Friction no funil de conversão — usuário quer criar conta, não entrar.
- **Fix:** Alterar `primaryCta={{ to: '/login' }}` para `to: '/cadastro'` nas linhas 392 e 476.

### P1-3: CTA "Começar Grátis" da FuncionalidadesPage aponta para `/login`

- **Arquivo:** `src/pages/public/FuncionalidadesPage.tsx:219`
- **Problema:** `buttonHref="/login"` com texto "Começar Grátis" — inconsistência com o funil de conversão.
- **Impacto:** Mesmo friction de conversão.
- **Fix:** Alterar `buttonHref` para `/cadastro`.

### P1-4: Ícones decorativos sem `aria-hidden` no ContactPage

- **Arquivo:** `src/pages/public/ContactPage.tsx:72-116` e `:430`
- **WCAG:** 1.1.1 Non-text Content (Level A)
- **Problema:** 7 ícones decorativos (EmailIcon, AccessTimeIcon, LanguageIcon, InstagramIcon, YouTubeIcon, XIcon + hero EmailIcon) sem `aria-hidden="true"`. Screen readers anunciam nome do ícone redundante.
- **Fix:** Adicionar `aria-hidden="true"` a todos os 7 ícones.

### P1-5: Ícones decorativos sem `aria-hidden` no StatusPage

- **Arquivo:** `src/pages/public/StatusPage.tsx:83-86` (STATUS_CONFIG), `:157`, `:230`, `:318`
- **WCAG:** 1.1.1 Non-text Content (Level A)
- **Problema:** Ícones decorativos sem `aria-hidden`. Nota: os 4 ícones em `STATUS_CONFIG` são passados como `icon` do Chip MUI — o Chip aplica `aria-hidden` automaticamente. Os outros 3 (VerifiedUserIcon, Timeline, AccessTimeIcon) usados diretamente não têm tratamento.
- **Fix:** Adicionar `aria-hidden="true"` aos 3 ícones usados diretamente (linhas 157, 230, 318). Os 4 em STATUS_CONFIG podem ser ignorados (MUI Chip trata automaticamente).

### P1-6: Navegação do header não inclui link para "Contato"

- **Arquivo:** `src/components/public/PublicHeader.tsx:47-53`
- **Problema:** Header tem 5 links (Home, Funcionalidades, Preços, FAQ, Sobre), mas "Contato" só aparece no footer.
- **Impacto:** Usuário precisa rolar toda a página para encontrar contato.
- **Fix:** Adicionar `{ to: '/contato', label: 'Contato' }` ao `PUBLIC_NAV_ITEMS`.

---

## P2 — Suggestion

### P2-1: Links ativos na navegação sem `aria-current="page"`

- **Arquivo:** `src/components/public/PublicHeader.tsx:147-163` (desktop) e `:271-289` (drawer)
- **WCAG:** 2.4.8 Location (Level AAA — boa prática, não obrigatório)
- **Problema:** Links ativos sem `aria-current="page"`. Screen readers não têm indicação programática de qual página está ativa.
- **Fix:** Adicionar `aria-current={isActive ? 'page' : undefined}` nos Buttons e ListItemButtons.

### P2-2: Contraste do `#2E75B6` como texto em dark theme (4.12:1)

- **Arquivo:** `src/theme/tokens.ts:29` + `FaqPage.tsx:269`, `ContactPage.tsx:243`, `PricingPage.tsx:190`
- **WCAG:** 1.4.3 Contrast (Minimum) — Level AA
- **Problema:** `#2E75B6` (brand primary) sobre `#050816` tem contraste 4.12:1, abaixo do mínimo 4.5:1 para texto normal. Alguns usos são em elementos interativos (tabs, botões) onde 3:1 basta.
- **Fix:** Usar `BRAND_PRIMARY_LIGHT` (#5BA3D0, ~7.21:1 contraste) para contexto de texto. Aceitável manter em hover/states transitórios.

### P2-3: `useCallback` desnecessário em `getPrice` do PricingPage

- **Arquivo:** `src/pages/public/PricingPage.tsx:357-362`
- **Problema:** `getPrice` envolvido em `useCallback` mas é invocado imediatamente no render (`price={getPrice(plan)}`). Não é prop memoizado nem dependency de hook. Zero benefício.
- **Fix:** Remover `useCallback`, definir como função simples.

### P2-4: Dead exports em `animations.ts` (4 variantes nunca importadas)

- **Arquivo:** `src/components/public/animations.ts:65-132`
- **Problema:** `slideInLeft`, `slideInRight`, `heroContainer`, `showcaseContainer` exportados mas nunca importados. Confirmado via grep.
- **Fix:** Remover os 4 exports.

### P2-5: SocialProofBar "Powered by Gemini AI" — prova de tecnologia, não de produto

- **Arquivo:** `src/pages/public/LandingPage.tsx:132-135`
- **Problema:** "Powered by Gemini AI" é statement tecnológico, não prova social. Sem métricas de adoção.
- **Fix:** Substituir por métricas de uso quando disponíveis. Decisão de marketing atual.

### P2-6: Textos técnicos na FuncionalidadesPage — jargão para público criador

- **Arquivo:** `src/pages/public/FuncionalidadesPage.tsx:27-62`
- **Problema:** Cards usam "chunking", "BFS", "WASM", "IndexedDB" que o público-alvo (criadores de conteúdo) provavelmente não entende.
- **Fix:** Reescrever descrições em linguagem orientada a benefícios.

### P2-7: "Em breve" no card Equipe sem caminho de conversão

- **Arquivo:** `src/pages/public/PricingPage.tsx:129-131`
- **Problema:** Botão "Em breve" disabled sem waitlist ou CTA alternativo. Visitante empresarial sem saída.
- **Fix:** Adicionar CTA funcional quando próximo do lançamento.

### P2-8: StepCard sem `glassPanelSx` — inconsistência visual com FeatureCard

- **Arquivo:** `src/components/public/StepCard.tsx:39-49`
- **Problema:** StepCard usa `backgroundColor: paper` opaco enquanto FeatureCard usa `glassPanelSx` (blur + gradiente + shadow). Coexistem na mesma página.
- **Fix:** Aplicar `...glassPanelSx(theme)` no Paper do StepCard.

### P2-9: CTASection — botão laranja com glow azul

- **Arquivo:** `src/components/public/CTASection.tsx:90-95`
- **Problema:** Botão `color="secondary"` (laranja) com `boxShadow` usando `BRAND_PRIMARY_GLOW` (azul). HeroSection faz certo com `BRAND_SECONDARY_GLOW_SOFT`.
- **Fix:** Trocar `BRAND_PRIMARY_GLOW` por `BRAND_SECONDARY_GLOW_SOFT` no estado padrão.

### P2-10: Sombras de hover inconsistentes entre cards

- **Arquivo:** `StepCard.tsx:47`, `AboutPage.tsx:201`, `AboutPage.tsx:244`
- **Problema:** FeatureCard usa `rgba(SHADOW_DEEP, 0.65)` enquanto StepCard/ValueCard/TeamCard usam `rgba(black, 0.2-0.3)` — imperceptível em dark theme.
- **Fix:** Unificar usando `SHADOW_DEEP` com opacidade 0.4-0.6 para todos os cards.

### P2-11: `pulseGlow` ignora `prefers-reduced-motion`

- **Arquivo:** `src/pages/public/AboutPage.tsx:311-315`
- **Problema:** Animação CSS infinita sem media query `prefers-reduced-motion: reduce`.
- **Fix:** Adicionar `@media (prefers-reduced-motion: reduce) { animation: none !important; }`.

---

## P3 — Nitpick

### P3-1: Return type `ReactNode` em componentes — tipo excessivamente amplo

- **Arquivo:** `CookiesPage.tsx:54`, `PrivacyPage.tsx:78`, `TermsPage.tsx:78`, `FAQAccordion.tsx:43`
- **Problema:** `: ReactNode` permite retornar `null`, strings, etc. Remover anotação e deixar TypeScript inferir `JSX.Element`.

### P3-2: Tipo `FAQItem` duplicado entre FaqPage e FAQAccordion

- **Arquivo:** `FaqPage.tsx:30` e `FAQAccordion.tsx:30`
- **Problema:** Interface idêntica definida em dois arquivos. Viola DRY.
- **Fix:** Exportar de FAQAccordion e importar em FaqPage.

### P3-3: Padrão de gradiente de texto repetido em 5 arquivos

- **Arquivo:** `HeroSection.tsx`, `CTASection.tsx`, `SocialProofBar.tsx`, `NotFoundPage.tsx`, `SpeedPaintPage.tsx`
- **Problema:** Bloco `background/clip/textFillColor` copiado 5 vezes.
- **Fix:** Criar token `TEXT_GRADIENT_SX` em tokens.ts.

### P3-4: `EMAIL_REGEX` dentro do componente (deveria ser do módulo)

- **Arquivo:** `src/pages/public/ContactPage.tsx:273`
- **Problema:** Regex constante definida dentro do `ContactForm`, recriada a cada render.
- **Fix:** Mover para escopo de módulo.

### P3-5: Tabela comparativa de preços com colunas hardcoded

- **Arquivo:** `src/pages/public/PricingPage.tsx:58-63,268-339`
- **Problema:** `ComparisonRow` com campos `gratuito`, `pro`, `equipe` hardcoded. Adicionar plano exige múltiplas edições.
- **Fix:** Usar `values: Record<string, string>` + array derivado de `PLANS`.

### P3-6: ComparisonTable `th` com `mb` inerte

- **Arquivo:** `src/pages/public/PricingPage.tsx:277-278`
- **Problema:** `mb: 0.5` em `<th>` é ignorado pelo CSS table layout. Margin não funciona em table cells.
- **Fix:** Remover `mb: 0.5` dos 4 `<th>`, usar `pb` se necessário.

---

## Falsos Positivos Eliminados

| Finding Original | Motivo |
|-----------------|--------|
| ~~P2-1 (variant="h6" component="p")~~ | Padrão MUI intencional — `variant` controla estilo, `component` controla semântica |
| ~~P2-2 (SocialProofBar h6/p)~~ | Mesmo padrão MUI de P2-1 |
| ~~P2-3 (TextField sem id)~~ | MUI v9 gera `id` automaticamente via `useId()` e conecta label via `htmlFor` |
| ~~P2-12 (FeatureShowcase spacing)~~ | `py={xs:6,md:10}` resulta em espaçamento equivalente às seções adjacentes |
| ~~P3-6 (roadmapItem)~~ | Variante genérica com delay por index, não page-specific |

## Reclassificações

| Finding | Original | Final | Motivo |
|---------|----------|-------|--------|
| FAQAccordion sem id/aria-controls | P0 | P1 | Real mas não quebra produção/crash |
| Links sem aria-current | P1 | P2 | WCAG 2.4.8 é Level AAA, não obrigatório |
| Contraste #2E75B6 | P1 (3.87:1) | P2 (4.12:1) | Valor real é 4.12:1, próximo do threshold |

---

## Pontos Fortes

- **Heading hierarchy correta** — todas as páginas têm exatamente um h1
- **SEO consistente** — `DocumentHead` + `getPageSeo()` em todas as 10 páginas, OG tags, JSON-LD
- **Sem `any`** — tipagem forte em todo o escopo
- **Skip-to-content funcional** com `href="#main-content"`
- **Landmarks completos** — header, nav, main, footer com roles corretos
- **Drawer com focus trap** automático do MUI
- **Touch targets** mínimos 44x44px
- **Links internos corretos** — todas as rotas correspondem ao router
- **LegalPageTemplate** elimina duplicação entre Terms/Privacy/Cookies
- **Separação dados/renderização** em todos os componentes
