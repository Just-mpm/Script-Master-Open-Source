# Audit de UI Design — Consolidado (Validado)

**Data:** 2026-04-26
**Escopo:** `src/` — UI Design completo (theme, components, pages, features)
**Agentes:** 3 audit-ui-design + validator-audit (cascata)
**Validação:** Cascata (3→1) — código real lido para cada finding

## Resumo

| Prioridade | Count | Descrição |
|------------|-------|-----------|
| P1 | 2 | Gradiente quebrado em produção, classes MUI v9 removidas |
| P2 | 6 | Tokens hardcoded, DRY violations, cor hardcoded |
| P3 | 11 | Inconsistências visuais, oportunidades de refatoração |

**Total validados:** 19 findings (de 24 originais)
**Falsos positivos removidos:** 2 (`App P2-1` button radius — design intencional; `App P3-1` header breakpoints — progressive disclosure intencional)
**Duplicatas removidas:** 3 (App P1-3 + P2-3 mesclados — mesmo TextField; Public P2-1 mesma causa-raiz de P1-1)

---

## P1

### [P1-1] FeatureShowcase — gradiente de fundo do ícone não renderiza

- **Arquivo:** `src/components/public/FeatureShowcase.tsx:86`
- **Tipo:** MUI (uso incorreto de propriedade CSS)
- **Confiança:** 98%
- **Descrição:** `bgcolor` no sx prop mapeia para `background-color`, que não aceita `linear-gradient(...)`. O ícone fica sem fundo gradiente — apenas com boxShadow e ícone branco sobre fundo transparente.
- **Impacto:** As 3 seções de FeatureShowcase na LandingPage e FuncionalidadesPage exibem ícones sem o fundo gradiente azul→laranja pretendido. Comparar com `StepCard.tsx:63` que usa `background: BRAND_GRADIENT` (correto).
- **Sugestão:** Trocar `bgcolor` por `background` na linha 86.

### [P1-2] FaqPage — classes CSS removidas do MUI v9

- **Arquivo:** `src/pages/public/FaqPage.tsx:237,283`
- **Tipo:** MUI (classes removidas no v9)
- **Confiança:** 99% (confirmado via NotebookLM MUI v9 Docs)
- **Descrição:** `.MuiTabs-flexContainer` (removida → usar `.MuiTabs-list`) e `.MuiTab-iconWrapper` (removida → usar `.MuiTab-icon`) não existem no MUI v9. Os estilos de gap entre tabs e espaçamento do ícone não estão sendo aplicados.
- **Impacto:** A navegação por categorias do FAQ perde o gap e o espaçamento do ícone, ficando visualmente amontoada. Os estilos podem ter parado de funcionar silenciosamente.
- **Sugestão:** Substituir `& .MuiTabs-flexContainer` → `& .MuiTabs-list` e `& .MuiTab-iconWrapper` → `& .MuiTab-icon`.

---

## P2

### [P2-1] FeatureCard variante `highlighted` — mesmo bug `bgcolor` com gradiente

- **Arquivo:** `src/components/public/FeatureCard.tsx:78`
- **Tipo:** MUI (mesma causa-raiz de P1-1)
- **Confiança:** 90%
- **Descrição:** Quando `highlighted={true}`, o `bgcolor` recebe `linear-gradient(...)` — inválido para `background-color`. A variante não é usada atualmente no projeto, mas está quebrada para uso futuro.
- **Impacto:** Nenhum impacto visual hoje (nenhuma FeatureCard passa `highlighted={true}`). Bug latente.
- **Sugestão:** Trocar `bgcolor` por `background` na linha 78.

### [P2-2] StepCard — cor hexadecimal hardcoded em vez de token

- **Arquivo:** `src/components/public/StepCard.tsx:88`
- **Tipo:** Missing token
- **Confiança:** 95%
- **Descrição:** `alpha('#F7941E', 0.3)` usa hex hardcoded. O token `BRAND_SECONDARY` (`'#F7941E'`) já está importado no mesmo arquivo (linha 8 indiretamente via `BRAND_GRADIENT`), embora não importado diretamente.
- **Impacto:** Se `BRAND_SECONDARY` mudar, o boxShadow do badge numérico ficará desatualizado.
- **Sugestão:** Importar `BRAND_SECONDARY` de `tokens.ts` e usar `alpha(BRAND_SECONDARY, 0.3)`.

### [P2-3] ErrorBoundary e NotFoundPage — duplicam manualmente glassPanelSx

- **Arquivo:** `src/components/ErrorBoundary.tsx:84-93` e `src/pages/NotFoundPage.tsx:42-51`
- **Tipo:** DRY violation
- **Confiança:** 88%
- **Descrição:** Ambos replicam manualmente as propriedades de `glassPanelSx` (borderRadius, border, backgroundImage, backdropFilter, boxShadow). `glassPanelSx` inclui `position: 'relative'` e `overflow: 'hidden'` que podem não ser desejados aqui — a substituição direta precisa desses overrides.
- **Impacto:** Mudanças no design system (`surfaces.ts`) não propagam para estes componentes.
- **Sugestão:** Usar `{...glassPanelSx(theme), position: 'static', overflow: 'visible'}` ou extrair as propriedades compartilhadas em uma função `glassBaseSx()`.

### [P2-4] authTextFieldSx e authLinkSx duplicados entre LoginPage e RegisterPage

- **Arquivo:** `src/pages/LoginPage.tsx:46-91` e `src/pages/RegisterPage.tsx:42-87`
- **Tipo:** DRY violation
- **Confiança:** 95%
- **Descrição:** Os blocos são idênticos (46 linhas cada). Ambos os arquivos já possuem comentários indicando "Estilos compartilhados entre LoginPage e RegisterPage".
- **Impacto:** Qualquer mudança no estilo de auth exige edição em dois lugares. Risco de divergência.
- **Sugestão:** Extrair para `src/theme/authStyles.ts` e importar em ambos.

### [P2-5] Library e ImageStudio — TextField focus usa cor hardcoded `#2E75B6` + ring inconsistente

- **Arquivo:** `src/components/Library.tsx:301,304` e `src/components/ImageStudio.tsx:366,369`
- **Tipo:** Hardcoded color + inconsistency
- **Confiança:** 92%
- **Descrição:** Ambos usam `borderColor: '#2E75B6'` (deveria ser `BRAND_PRIMARY`) no focus. Além disso, o boxShadow de focus usa valores diferentes: Library usa `0.15`, ImageStudio usa `0.12` — deveriam ser idênticos. Padrão também aparece no LoginPage com `0.12`.
- **Impacto:** Se a cor primária mudar, estes campos ficam defasados. O anel de foco tem intensidade diferente entre componentes.
- **Sugestão:** Substituir `'#2E75B6'` por `BRAND_PRIMARY`. Criar token `BRAND_PRIMARY_FOCUS_RING` com valor consistente.

### [P2-6] AssistantComposer — cor de erro hardcoded `#ef4444` em vez de token

- **Arquivo:** `src/features/assistant/components/AssistantComposer.tsx:149,153`
- **Tipo:** Missing token
- **Confiança:** 90%
- **Descrição:** O botão "Stop" usa `alpha('#ef4444', 0.24)` e `alpha('#ef4444', 0.36)`. O token `ERROR_MAIN` é `'#ef4444'` — deveria ser usado.
- **Impacto:** Se a cor de erro mudar, o boxShadow do botão ficará desalinhado.
- **Sugestão:** Substituir `'#ef4444'` por `ERROR_MAIN`.

---

## P3

### [P3-1] StepCard sem `glassPanelSx` — inconsistência visual com FeatureCard

- **Arquivo:** `src/components/public/StepCard.tsx:37-48`
- **Tipo:** Consistency
- **Confiança:** 85%
- **Descrição:** StepCard usa `Paper variant="outlined"` com estilos inline básicos, sem `glassPanelSx`. FeatureCard e PricingCard na mesma página aplicam `glassPanelSx`. StepCards ficam opacos ao lado de FeatureCards com efeito glass premium.
- **Impacto:** Na LandingPage, a seção "Como Funciona" (3 StepCards) contrasta visualmente com a seção de features (6 FeatureCards).
- **Sugestão:** Aplicar `glassPanelSx` ao StepCard para consistência visual.

### [P3-2] Duplicate `@keyframes pulse` — definições conflitantes

- **Arquivo:** `src/index.css:82-85` e `src/pages/public/AboutPage.tsx:312-314`
- **Tipo:** Consistency
- **Confiança:** 88%
- **Descrição:** `index.css` define animação de opacity (1→0.4→1). `AboutPage.tsx` define animação de boxShadow (glow pulse) via sx scoped. Não quebra hoje (sx keyframes são escopados pelo Emotion), mas é uma armadilha de manutenção — nome ambíguo para animações semanticamente diferentes.
- **Impacto:** Nenhum impacto visual hoje. Qualquer uso futuro de `animation: pulse` via CSS global obterá a versão errada.
- **Sugestão:** Renomear a animação do AboutPage para `pulseGlow`.

### [P3-3] ContactPage — botões de redes sociais sem borda visível

- **Arquivo:** `src/pages/public/ContactPage.tsx:227-244`
- **Tipo:** Consistency
- **Confiança:** 88%
- **Descrição:** Os botões não declaram `variant="outlined"`, então renderizam como `variant="text"`. Os estilos `borderColor` no sx são código morto — sem `variant="outlined"`, o MUI Button não renderiza borda.
- **Impacto:** Os botões aparecem como texto puro sem borda. A transição `border-color` no hover nunca é visível.
- **Sugestão:** Adicionar `variant="outlined"` ao Button.

### [P3-4] VideoExportPanel — estado de sucesso sem responsividade

- **Arquivo:** `src/features/video-render/components/VideoExportPanel.tsx:484-500`
- **Tipo:** Responsive
- **Confiança:** 82%
- **Descrição:** A row de sucesso contém 3 botões + texto de status sem `flexWrap` ou mudança de `direction` em breakpoints. Em telas estreitas (~360px), os botões podem transbordar. `TranscriptionPanel.tsx:149` resolve o mesmo cenário com `direction={{ xs: 'column', sm: 'row' }}`.
- **Impacto:** Em mobile, os 3 botões + status podem exceder a largura do painel.
- **Sugestão:** Adicionar `direction={{ xs: 'column', sm: 'row' }}` na Stack externa.

### [P3-5] Barra de progresso com 3 implementações visuais distintas

- **Arquivo:** `src/features/speed-paint/components/canvas/AnimationPlayer.tsx:135`, `src/features/video-render/components/VideoExportPanel.tsx:458`, `src/features/video-render/components/TranscriptionPanel.tsx:213`
- **Tipo:** Consistency
- **Confiança:** 87%
- **Descrição:** AnimationPlayer usa height:10 + flat `primary.main`. VideoExport e Transcription usam height:8 + `BRAND_GRADIENT`. Divergência de tamanho, cor e estilo entre páginas.
- **Impacto:** Usuário que navega entre Speed Paint e Vídeo percebe barras de progresso visualmente diferentes.
- **Sugestão:** Criar constante `PROGRESS_BAR_SX` compartilhada unificando os estilos.

### [P3-6] Header — borderColor hardcoded vs token `APP_BORDER_STRONG`

- **Arquivo:** `src/components/Header.tsx:272`
- **Tipo:** Missing token
- **Confiança:** 88%
- **Descrição:** `borderColor: 'rgba(255, 255, 255, 0.14)'` no hover do avatar section. O token `APP_BORDER_STRONG` é exatamente esse valor.
- **Impacto:** Baixo impacto funcional, mas viola o princípio de usar tokens.
- **Sugestão:** Substituir por `APP_BORDER_STRONG`.

### [P3-7] Focus ring alpha inconsistente entre 3 componentes

- **Arquivo:** `src/components/Library.tsx:304` (0.15), `src/components/ImageStudio.tsx:369` (0.12), `src/pages/LoginPage.tsx:432` (0.12)
- **Tipo:** Consistency
- **Confiança:** 85%
- **Descrição:** O mesmo padrão `0 0 0 3px rgba(46, 117, 182, X)` tem 3 valores diferentes de alpha. O token `ACTION_FOCUS` usa `0.22` (mais intenso), mas nenhum componente o utiliza.
- **Impacto:** O anel de foco tem intensidade diferente entre TextField de busca, estúdio de imagem e login.
- **Sugestão:** Criar token `BRAND_PRIMARY_FOCUS_RING` com valor consistente (ex: `0.12`).

### [P3-8] `fontFamily: 'JetBrains Mono, monospace'` hardcoded em 17+ locais

- **Arquivo:**ActionBar (6), CaptionEditorPanel (3), AnimationControls (1), TranscriptionPanel (1), VideoExportPanel (1), SpeedPaintControls (2), App.tsx (1), PricingCard (1), SubtitlePreview (1), DragOverlay (1)
- **Tipo:** Missing token
- **Confiança:** 85%
- **Descrição:** A string `'JetBrains Mono, monospace'` é repetida em 17+ locais. `index.css` define `--font-mono` como variável CSS, mas nenhum componente a referencia.
- **Impacto:** Se a fonte mono mudar, precisa buscar e substituir em 17+ arquivos. Inconsistência entre `"JetBrains Mono"` (com aspas) e `JetBrains Mono` (sem).
- **Sugestão:** Criar token `FONT_MONO = 'JetBrains Mono, monospace'` em `tokens.ts` ou usar `var(--font-mono)`.

### [P3-9] SpeedPaintPhaseBadge — cor azul fora da paleta da marca

- **Arquivo:** `src/features/video-render/components/SpeedPaintScene.tsx:195`
- **Tipo:** Missing token
- **Confiança:** 87%
- **Descrição:** Badge "Colorir..." usa `rgba(91, 154, 255, 0.15)` — azul claro (#5B9AFF) que não corresponde a `BRAND_PRIMARY` (#2E75B6). Badge é pequeno e com opacity baixa, mas tecnicamente inconsistente.
- **Impacto:** Subtil — badge pequeno, opacity baixa.
- **Sugestão:** Usar `alpha(BRAND_PRIMARY, 0.15)` ou variante baseada no token.

### [P3-10] QueueStaging — chamadas `alpha()` em vez de tokens existentes

- **Arquivo:** `src/features/speed-paint/components/batch/QueueStaging.tsx:82,110`
- **Tipo:** Missing token
- **Confiança:** 85%
- **Descrição:** `alpha(theme.palette.common.white, 0.06)` em linhas 82 e 110 é equivalente ao token `WHITE_06`.
- **Impacto:** Nenhum impacto visual (valores idênticos), mas reduz rastreabilidade.
- **Sugestão:** Importar e usar `WHITE_06`.

### [P3-11] CaptionEditorPanel — boxShadow de hover com valores inline

- **Arquivo:** `src/features/video-render/components/CaptionEditorPanel.tsx:257,272`
- **Tipo:** Missing token
- **Confiança:** 84%
- **Descrição:** Botões confirmar/cancelar usam `rgba(16, 185, 129, 0.12)` e `rgba(239, 68, 68, 0.12)`. Os tokens `SUCCESS_GLOW` (0.2) e `ERROR_GLOW` (0.15) existem mas com valores diferentes. A diferença pode ser intencional (glow mais suave para botões pequenos).
- **Impacto:** Inconsistência sutil entre glow de botões do CaptionEditor vs tokens usados no subtitle-editor (0.14 e 0.15).
- **Sugestão:** Importar tokens ou documentar por que os valores são intencionalmente diferentes.

---

## Falsos Positivos Rejeitados

| Finding Original | Motivo da Rejeição | Confiança |
|-----------------|-------------------|-----------|
| App P2-1 — Border radius Button (14) vs glassPanel (3-4) | Design intencional — botões se destacam dos painéis glass. O próprio auditor nota ser "trade-off de design provável". | 60% |
| App P3-1 — Breakpoint hierarchy no Header | Progressive disclosure intencional. O auditor reconhece "funcionalmente OK". | 65% |

---

## Nota sobre Severity

Regras aplicadas na reclassificação:
- **P1→P2:** Findings de DRY/maintenance sem impacto visual imediato (ErrorBoundary glassPanelSx, auth styles duplicados, TextField hardcoded color)
- **P1→P3:** Findings de consistência visual sem bug funcional (StepCard sem glass, duplicate keyframes)
- **P2→P3:** Findings com confidence 80-89% rebaixados conforme gate de confiança
