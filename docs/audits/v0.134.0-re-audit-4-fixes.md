# Re-Auditoria de Correções — Release v0.134.0 (4 Fixes)

**Data:** 2026-07-28
**Auditor:** Code Validator
**Motivo:** Verificar 4 correções aplicadas após rodada anterior de auditores.
**Validações externas:** `lint` → 0 | `typecheck` → 0 | `test` → 2613/2613 ✅

---

## 1. Escopo da Revisão

### Arquivos lidos por completo (8)

| # | Arquivo | Papel |
|---|---------|-------|
| 1 | `src/theme/animations.ts` | JSDoc corrigido — exemplo de uso com array syntax |
| 2 | `src/theme/surfaces.ts` | JSDoc corrigido — 3 consumidores do `appDrawerPaperSx` |
| 3 | `tests/video-render/SpeedPaintControls.unit.test.tsx` | Mock `glassPanelSx` como função (tipo correto) |
| 4 | `tests/i18n/i18n-used-keys.unit.test.ts` | Timeout aumentado 15_000 → 30_000 |
| 5 | `src/components/app/SidebarNavItem.tsx` | Consome `exportDotPulseKeyframes` via array syntax |
| 6 | `src/components/app/MobileBottomNav.tsx` | Consome ambos os helpers, Drawer com `appDrawerPaperSx` |
| 7 | `src/components/app/GuestMobileNav.tsx` | Consome `appDrawerPaperSx` |
| 8 | `src/components/public/PublicHeader.tsx` | Consome `appDrawerPaperSx` |

### Focos cobertos

- ✅ 4 correções verificadas individualmente (código real)
- ✅ Consistência de consumo dos helpers nos 3 Drawers
- ✅ Tipagem de mocks (função vs objeto)
- ✅ Padrão array syntax do MUI v9 (confirmado via NotebookLM)
- ✅ Varredura de problemas residuais não cobertos
- ✅ Notebook MUI v9 consultado (`648c498b`): array syntax confirmado como padrão oficial

---

## 2. Veredito

**Sem problemas pendentes.** As 4 correções estão aplicadas corretamente. Nenhum novo problema foi introduzido. Nenhum WARNING ou CRITICAL não coberto foi encontrado. A release pode seguir.

---

## 3. Achados Detalhados

### Item 1 ✅ — JSDoc do `exportDotPulseKeyframes` corrigido

**Arquivo:** `src/theme/animations.ts:16-19`
**Categoria:** Documentation
**Confidence:** 100/100

**Problema original:** JSDoc usava `mergeSx({...}, exportDotPulseKeyframes)` (função inexistente).

**Evidência da correção:**
```typescript
// Linhas 16-19:
 * Uso (array syntax do MUI v9, padrão oficial para SxProps compartilhados):
 * ```tsx
 * <Box sx={[exportDotPulseKeyframes, { animation: 'exportDotPulse 1.6s ease-in-out infinite' }]} />
 * ```
```

**Verificação:** NotebookLM MUI v9 confirma que array syntax é o padrão oficial para mesclar SxProps. A constante continua exposta para casos que precisem spread manual (linhas 21-23), o que é uma documentação precisa — não incentivando uso inadequado.

**Validação cruzada:**
- `SidebarNavItem.tsx:172-189` — `sx={[exportDotPulseKeyframes, {...}]}` ✅ array syntax
- `MobileBottomNav.tsx:284-301` — `sx={[exportDotPulseKeyframes, {...}]}` ✅ array syntax

**Nenhum componente usa spread do `exportDotPulseKeyframes`.** Ambos consomem via array syntax.

---

### Item 2 ✅ — JSDoc do `appDrawerPaperSx` corrigido

**Arquivo:** `src/theme/surfaces.ts:43-57`
**Categoria:** Documentation
**Confidence:** 100/100

**Problema original:** JSDoc listava apenas 2 consumidores (faltava `PublicHeader`). Menção imprecisa a "borderRight extra".

**Evidência da correção:**
```typescript
// Linhas 43-51:
/**
 * Estilo glass compartilhado para o `Paper` de Drawers laterais de navegação
 * (`MobileBottomNav`, `GuestMobileNav` e `PublicHeader`).
 *
 * Centraliza `backgroundColor`, gradiente sutil e `borderRight` para que os
 * Drawers tenham exatamente a mesma aparência de superfície. Drawers que
 * precisem de personalização adicional (ex: `MobileBottomNav` define
 * `width: 280` para a largura do drawer mobile) devem estender este objeto
 * via spread em vez de redefinir as três chaves básicas.
 */
```

**Validação cruzada dos 3 consumidores:**

| Componente | Arquivo | Linha | Uso | Estende? |
|---|---|---|---|---|
| `MobileBottomNav` | `MobileBottomNav.tsx` | 342 | `{ ...appDrawerPaperSx, width: 280 }` | ✅ spread + `width` |
| `GuestMobileNav` | `GuestMobileNav.tsx` | 101 | `sx: appDrawerPaperSx` | ✅ sem extensão |
| `PublicHeader` | `PublicHeader.tsx` | 291 | `sx: appDrawerPaperSx` | ✅ sem extensão |

Todos os 3 importam e consomem corretamente.

---

### Item 3 ✅ — Mock do `SpeedPaintControls` alinhado

**Arquivo:** `tests/video-render/SpeedPaintControls.unit.test.tsx:8-12`
**Categoria:** Test
**Confidence:** 100/100

**Problema original:** `glassPanelSx: {}` (objeto), tipo incompatível com o export real `(theme: Theme) => SystemStyleObject<Theme>` (função).

**Evidência da correção:**
```typescript
vi.mock('../../src/theme/surfaces', () => ({
  insetPanelSx: () => ({ p: 2 }),
  glassPanelSx: () => ({}),       // ← função, tipo correto
  appDrawerPaperSx: {},           // ← objeto (correto)
}));
```

**Comparação com o factory centralizado (`tests/__mocks__/surfacesMock.ts:29-35`):**
```typescript
export const surfacesMock = {
  glassPanelSx: (): Record<string, unknown> => ({}),
  insetPanelSx: (): Record<string, unknown> => ({}),
  glassSurfaceSx: (): Record<string, unknown> => ({}),
  appDrawerPaperSx: {} as Record<string, unknown>,
  searchFieldSx: {} as Record<string, unknown>,
};
```

O `glassPanelSx` alinhou com o factory (função). `insetPanelSx` mantém stub personalizado (`{ p: 2 }`) — é escape hatch justificado (este teste precisa de padding específico). O `appDrawerPaperSx` também está alinhado (objeto).

**Grep confirmou:** Nenhum `glassPanelSx` objeto (`: {}`) solto em outros arquivos. Todos são função.

---

### Item 4 ✅ — Timeout do teste i18n aumentado

**Arquivo:** `tests/i18n/i18n-used-keys.unit.test.ts:87`
**Categoria:** Test / Flakiness
**Confidence:** 100/100

**Problema original:** `timeout: 15_000` — flakeava sob carga da suíte completa (~4-5min).

**Evidência da correção:**
```typescript
it('mantém toda chave literal usada em t(...) existente no locale base', { timeout: 30_000 }, () => {
  // Timeout elevado (default 15s flakeava sob carga da suíte completa ~4-5min).
  // Esse teste varre todos os arquivos `src/` buscando chamadas `t(...)` — custo
  // O(n) na quantidade de arquivos. Tolerância de 30s é folgado para a suíte
  // completa e ainda alerta regressões genuínas.
```

**Justificativa:** O teste varre todo `src/` com TypeScript AST — custo O(n) na quantidade de arquivos. 30s é tolerância folgada que ainda detecta regressões genuínas (ex: se o scan começar a levar 60s+, algo está errado). Comentário claro e auto-explicativo.

---

## 4. Correções Extras Encontradas (não listadas como item, mas benéficas)

### ✅ `aria-controls` adicionado ao MobileBottomNav (auditoria anterior)

O WARNING reportado em `docs/audits/2026-07-28_drawers-a11y-mocks-audit.md` sobre falta de `aria-controls` no trigger do Drawer foi corrigido:

- **Trigger** (linha 323): `aria-controls="mobile-bottom-drawer"` ✅
- **Drawer** (linha 345): `id="mobile-bottom-drawer"` ✅

Doravante `GuestMobileNav` (linhas 84/102) e `PublicHeader` (linhas 230/292) já estavam corretos.

### ✅ `AnalyticsConsentPrompt` — `actionPlacement="bottom"`

Mudança de `"stack"` para `"bottom"` bem documentada (JSDoc linhas 76-84) com justificativa validada pelo Notebook MUI v9. Sem problemas.

---

## 5. Varredura de Problemas Não Cobertos

### 🔍 Nenhum WARNING ou CRITICAL não resolvido encontrado

| Tipo | Resultado |
|------|-----------|
| `any` ou casting inseguro | Nenhum encontrado nos arquivos alterados |
| Race condition | Nenhum — estado local consistente, handlers em sequência |
| Memory leak | Nenhum — listeners com cleanup, sem closures problemáticas |
| Dead code | Nenhum |
| Arquitetura quebrada | Responsabilidades bem separadas entre Drawers |
| Segurança | Nenhum segredo ou validação fraca |
| Performance | Nenhum re-render desnecessário identificado |
| UX/UI | Estados loading/erro/vazio adequados |
| i18n | Chaves existentes nos 3 locales |
| Tipos TypeScript | Sem `as any`, `@ts-ignore` ou `@ts-expect-error` |

### 🔍 Mocks — 5 escape hatches documentados

Os 5 testes que mantêm mocks inline (não usam `surfacesMock`) foram verificados:

| Arquivo | `glassPanelSx` | `appDrawerPaperSx` | Status |
|---|---|---|---|
| `assistantUi.unit.test.ts` | — (não mocka) | `{}` | ✅ Objeto, correto |
| `ConfiguracoesPage.component.test.tsx` | — (não mocka) | `{}` | ✅ Objeto, correto |
| `SpeedPaintControls.unit.test.tsx` | `() => ({})` | `{}` | ✅ Função + Objeto |
| `CaptionEditorPanel.unit.test.tsx` | — (não mocka) | `{}` | ✅ Objeto, correto |
| `VideoExportPanel.unit.test.tsx` | `() => ({})` | `{}` | ✅ Função + Objeto |

Nenhum escape hatch tem tipo incorreto.

---

## 6. O que parece saudável

- **Separação clara de responsabilidades:** cada Drawer controla seu próprio estado e diálogos
- **Documentação de JSDoc precisa e atualizada** nos helpers compartilhados
- **Consumo via array syntax** do exportDotPulseKeyframes nos 2 componentes — aderente ao padrão MUI v9
- **Factory centralizado de mocks** (`surfacesMock.ts`) com JSDoc explicativo e justificativa da abordagem `async import`
- **5 escape hatches** com justificativa clara para manter mock inline
- **Comentários explicativos** nas mudanças (GAP-06, GAP-07, timeout, etc.)
- **Testes passando:** 2613/2613 ✅

---

## 7. Limites da Revisão

- Não foi verificado o funcionamento em runtime (apenas leitura estática + typecheck)
- A mudança de `actionPlacement="stack"` → `"bottom"` no `AnalyticsConsentPrompt` foi assumida como correta com base no JSDoc e NotebookLM
- O escopo de 54 arquivos modificados foi coberto por amostragem focada nos 4 itens + consumidores diretos
- Não foi verificado o barrel export do theme (inexistente — imports são diretos)
- NotebookLM consultado apenas para MUI v9 (demais stacks não necessitaram)

---

## 8. Gate de Saída

- [x] Li o contexto mínimo real (8 arquivos por completo, greps de verificação)
- [x] Cada achado passou pela validação anti-falso-positivo
- [x] Cada achado passou pelo confidence gate numérico (todos ≥ 100/100)
- [x] Achados com confidence < 80 foram descartados (não houve)
- [x] O relatório está consolidado, priorizado e salvo em `docs/audits/`
- [x] Nenhum motivo real para escalar
