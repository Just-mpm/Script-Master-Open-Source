/**
 * Tipos do domínio de templates do estúdio.
 *
 * Re-exporta os tipos centrais de `scriptTemplates.ts` para consumo interno
 * dos componentes e utilitários do feature studio.
 */

export type { TemplateCategory, ScriptTemplate } from '../../../data/scriptTemplates';
